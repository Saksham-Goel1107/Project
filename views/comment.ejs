<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Comments</title>
  <link rel="stylesheet" href="/stylesheets/output.css">
</head>
<body class="bg-zinc-900 text-white p-6">
  <div class="max-w-xl mx-auto">
    <h2 class="text-xl font-semibold text-blue-400">@<%= post.user.name %>'s Post</h2>
    <p class="text-gray-300 mt-2"><%= post.content %></p>

    <% if (post.image) { %>
      <img src="<%= post.image %>" alt="Post Image"
        class="mt-3 w-full rounded-lg shadow-md border border-zinc-700">
    <% } %>

    <h3 class="mt-6 text-lg text-gray-300">Comments:</h3>
    <div class="mt-3 space-y-3">
      <% post.comments.forEach(function(comment) { %>
        <div class="p-3 bg-gray-800 rounded-md">
          <strong class="text-blue-300">@<%= comment.user.name %></strong>
          <p class="text-gray-400"><%= comment.text %></p>
        </div>
      <% }); %>
    </div>

 
    <form action="/comment/<%= post._id %>" method="POST" class="mt-4 flex items-center">
      <input type="text" name="text" placeholder="Write a comment..."
        class="flex-grow p-2 rounded-md bg-gray-700 text-white border border-gray-600 focus:outline-none">
      <button type="submit" class="ml-3 px-4 py-1 bg-blue-500 text-white rounded-md">Send</button>
    </form>

    <a href="/notes" class="mt-4 inline-block text-gray-400 hover:text-blue-300">← Back to Notes</a>
  </div>
</body>
</html>
