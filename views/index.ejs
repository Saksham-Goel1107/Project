<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Account</title>
    <link rel="stylesheet" href="/stylesheets/output.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body class="bg-zinc-900 text-white overflow-hidden">

   
    <div class="p-4">
        <%- include('partials/flash') %>
    </div>

    <div class="w-full min-h-screen flex flex-col justify-center items-center">
        
        <div class="bg-zinc-800 p-6 rounded-lg shadow-lg w-96">
            <h3 class="text-3xl mb-5 text-center font-bold">Create Account</h3>

            <form action="/register" method="post" class="flex flex-col gap-3">
                <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-600 rounded-md block w-full"
                    type="text" placeholder="Full Name" name="name" required>

                <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-600 rounded-md block w-full"
                    type="text" placeholder="Username" name="username" required>

                <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-600 rounded-md block w-full"
                    type="email" placeholder="Email" name="email" required>

                <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-600 rounded-md block w-full"
                    type="password" placeholder="Password" name="password" required>

                <input
                    class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-600 rounded-md block w-full no-arrow"
                    type="number" placeholder="Age" name="age" required>

                <button
                    class="px-3 py-2 bg-blue-500 hover:bg-blue-600 text-white font-bold rounded-md mt-3 w-full">Create
                    Account</button>
            </form>
        </div>

       
        <div class="mt-6 text-center">
            <p class="text-zinc-400">Already have an account?</p>
            <a class="text-blue-500 hover:underline font-medium" href="/login">Sign in</a>
        </div>
    </div>

</body>

</html>