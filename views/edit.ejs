<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post</title>
  <link rel="stylesheet" href="/stylesheets/output.css">
</head>
<body class="bg-zinc-900 text-white p-6 min-h-screen">
  
  <h2 class="text-2xl">Edit Your Post</h2>
  
  <form action="/update/<%= post._id %>" method="post">
    <textarea name="content"
      class="block w-full max-w-lg p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"
      required><%= post.content %></textarea>
    <button type="submit" 
      class="px-4 py-2 bg-green-500 hover:bg-green-600 text-white rounded-md mt-2 cursor-pointer">
      Update Post
    </button>
  </form>

  <a href="/profile" class="text-blue-400 hover:text-blue-300 mt-4 block">Back to Profile</a>

</body>
</html>
